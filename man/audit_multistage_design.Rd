% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multistage_audit.R
\name{audit_multistage_design}
\alias{audit_multistage_design}
\alias{print.sw_multistage_audit}
\title{Audit identifiability of an asynchronous cumulative stepped-wedge design}
\usage{
audit_multistage_design(
  design,
  delay_a = 0L,
  delay_b = 0L,
  adjust_sequence = FALSE
)

\method{print}{sw_multistage_audit}(x, ...)
}
\arguments{
\item{design}{An \code{\link[=sw_multistage_design]{sw_multistage_design()}} object.}

\item{delay_a, delay_b}{Non-negative integer delays used to form the default
analysis indicators. These should match the values in
\code{\link[=sw_multistage_assumptions]{sw_multistage_assumptions()}}.}

\item{adjust_sequence}{Logical; include sequence fixed effects in the
fixed-effect rank check.}

\item{x}{An \code{sw_multistage_audit} object.}

\item{...}{Unused.}
}
\value{
An object of class \code{"sw_multistage_audit"} containing overlap
periods, rank diagnostics, schedule flags, state counts, and explanatory
messages.
}
\description{
Checks whether a \code{Control -> A -> A+B} schedule provides the concurrent
comparisons needed to distinguish intervention effects from calendar time.
It also flags two common threats to interpretation: adding B in one common
calendar period, and adding B after exactly the same duration of A exposure
in every sequence. The latter can make an incremental B effect difficult to
distinguish from maturation of A when A itself may change with exposure time.
}
\examples{
design <- sw_multistage_design(
  clusters_per_sequence = rep(4, 4),
  a_start = c(2, 3, 4, 5),
  b_start = c(5, 7, 6, 8),
  n_periods = 8
)
audit_multistage_design(design, delay_a = 1, delay_b = 1)
}
