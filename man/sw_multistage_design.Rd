% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multistage_design.R
\name{sw_multistage_design}
\alias{sw_multistage_design}
\alias{print.sw_multistage_design}
\title{Construct an asynchronous cumulative-intervention stepped-wedge design}
\usage{
sw_multistage_design(
  clusters_per_sequence,
  a_start = NULL,
  b_start = NULL,
  n_periods = NULL,
  state = NULL,
  sequence_names = NULL
)

\method{print}{sw_multistage_design}(x, ...)
}
\arguments{
\item{clusters_per_sequence}{Positive integer vector giving the number of
clusters assigned to each sequence.}

\item{a_start}{Integer vector giving the first period under intervention A
for each sequence. Use \code{Inf} for a sequence that never starts A. Ignored
when \code{state} is supplied.}

\item{b_start}{Integer vector giving the first period under A+B for each
sequence. Each finite value must be strictly later than the corresponding
\code{a_start}. Use \code{Inf} for a sequence that never adds B. Ignored when \code{state}
is supplied.}

\item{n_periods}{Number of periods. If \code{NULL}, inferred from the largest
finite start period or the number of columns in \code{state}.}

\item{state}{Optional integer matrix with values \code{0}, \code{1}, and \code{2}, one row
per sequence and one column per period. This overrides \code{a_start} and
\code{b_start}.}

\item{sequence_names}{Optional unique labels for the sequences.}

\item{x}{An \code{sw_multistage_design} object.}

\item{...}{Unused.}
}
\value{
An object of class \code{"sw_multistage_design"}.
}
\description{
Builds a design in which clusters move through the ordered treatment states
\code{Control -> A -> A+B}. Sequences may start intervention A and add
intervention B in different calendar periods. B is therefore interpreted as
an add-on intervention delivered only after A has begun; the design contains
no B-only condition.
}
\details{
The design can be specified either by sequence-specific start periods or by
an explicit state matrix. In a state matrix, rows are sequences, columns are
periods, and entries are \code{0} (Control), \code{1} (A), or \code{2} (A+B). Every row must
be non-decreasing and must contain at least one A-only period before A+B.
}
\examples{
design <- sw_multistage_design(
  clusters_per_sequence = rep(6, 5),
  a_start = c(2, 3, 4, 5, 6),
  b_start = c(6, 7, 8, 9, 10),
  n_periods = 10
)
design
}
