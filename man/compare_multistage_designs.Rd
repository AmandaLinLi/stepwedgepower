% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multistage_compare.R
\name{compare_multistage_designs}
\alias{compare_multistage_designs}
\alias{print.sw_multistage_comparison}
\title{Compare two-state and cumulative three-state stepped-wedge designs}
\usage{
compare_multistage_designs(
  design_two,
  design_three,
  assumptions_two,
  assumptions_three = assumptions_two,
  labels = c("Control -> A", "Control -> A -> A+B"),
  nsim = 1000,
  alpha = 0.05,
  multiplicity = c("holm", "bonferroni", "none"),
  fit_link = c("logit", "identity"),
  nAGQ = 1,
  analysis_args = list(),
  n_cores = 1,
  seed = NULL,
  check_design = TRUE,
  warn_on_design = TRUE
)

\method{print}{sw_multistage_comparison}(x, ...)
}
\arguments{
\item{design_two, design_three}{Two \code{\link[=sw_multistage_design]{sw_multistage_design()}} objects. The
first would normally have \code{b_start = Inf} for every sequence.}

\item{assumptions_two}{A \code{\link[=sw_multistage_assumptions]{sw_multistage_assumptions()}} object for the first
design.}

\item{assumptions_three}{Assumptions for the second design. Defaults to
\code{assumptions_two}.}

\item{labels}{Two display labels.}

\item{nsim}{Number of simulated trials.}

\item{alpha}{Two-sided significance level.}

\item{multiplicity}{Adjustment for the A and incremental-B component tests:
\code{"holm"}, \code{"bonferroni"}, or \code{"none"}.}

\item{fit_link}{Link used in \code{\link[=fit_multistage_model]{fit_multistage_model()}}.}

\item{nAGQ}{Adaptive Gauss-Hermite quadrature points.}

\item{analysis_args}{Optional named list of additional arguments passed to
\code{\link[=fit_multistage_model]{fit_multistage_model()}}, such as \code{adjust_sequence} or \code{period_effect}.}

\item{n_cores}{Number of cores. Reproducibility under a fixed \code{seed} does not
depend on this value.}

\item{seed}{Optional random seed.}

\item{check_design}{Logical; audit the schedule before simulation and stop
for a rank-deficient or non-estimable default model.}

\item{warn_on_design}{Logical; emit non-fatal design-audit cautions.}

\item{x}{An \code{sw_multistage_comparison} object.}

\item{...}{Unused.}
}
\value{
An object of class \code{"sw_multistage_comparison"} containing both power objects, a stacked comparison table, a combined fit-diagnostics table, and a resource check.
}
\description{
Runs \code{\link[=power_multistage_swcrt]{power_multistage_swcrt()}} for two candidate schedules, commonly a
\code{Control -> A} design and an asynchronous \code{Control -> A -> A+B} design. The
function reports the operating characteristics side by side and checks
whether the designs use the same number of clusters, periods, and
cluster-period observations.
}
\details{
When the same \code{seed} is used, both designs start from the same reproducible
simulation streams. This can reduce simulation noise in a like-for-like
comparison when their dimensions match.
}
\examples{
\donttest{
three <- sw_multistage_design(
  clusters_per_sequence = rep(4, 4),
  a_start = c(2, 3, 4, 5), b_start = c(5, 7, 6, 8),
  n_periods = 8
)
two <- sw_multistage_design(
  clusters_per_sequence = rep(4, 4),
  a_start = c(2, 3, 4, 5), b_start = rep(Inf, 4),
  n_periods = 8
)
a <- sw_multistage_assumptions(
  baseline_prob = 0.15, treatment_or_a = 1.4,
  incremental_or_b = 1.3, delay_a = 1, delay_b = 1,
  icc = 0.05, n_per_cluster_period = 30
)
compare_multistage_designs(two, three, a, nsim = 100, seed = 1)
}
}
