% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multistage_power.R
\name{power_multistage_swcrt}
\alias{power_multistage_swcrt}
\title{Estimate power for an asynchronous Control to A to A+B design}
\usage{
power_multistage_swcrt(
  design,
  assumptions,
  nsim = 1000,
  alpha = 0.05,
  multiplicity = c("holm", "bonferroni", "none"),
  fit_link = c("logit", "identity"),
  nAGQ = 1,
  analysis_args = list(),
  n_cores = 1,
  seed = NULL,
  check_design = TRUE,
  warn_on_design = TRUE
)
}
\arguments{
\item{design}{An \code{\link[=sw_multistage_design]{sw_multistage_design()}} object.}

\item{assumptions}{An \code{\link[=sw_multistage_assumptions]{sw_multistage_assumptions()}} object.}

\item{nsim}{Number of simulated trials.}

\item{alpha}{Two-sided significance level.}

\item{multiplicity}{Adjustment for the A and incremental-B component tests:
\code{"holm"}, \code{"bonferroni"}, or \code{"none"}.}

\item{fit_link}{Link used in \code{\link[=fit_multistage_model]{fit_multistage_model()}}.}

\item{nAGQ}{Adaptive Gauss-Hermite quadrature points.}

\item{analysis_args}{Optional named list of additional arguments passed to
\code{\link[=fit_multistage_model]{fit_multistage_model()}}, such as \code{adjust_sequence} or \code{period_effect}.}

\item{n_cores}{Number of cores. Reproducibility under a fixed \code{seed} does not
depend on this value.}

\item{seed}{Optional random seed.}

\item{check_design}{Logical; audit the schedule before simulation and stop
for a rank-deficient or non-estimable default model.}

\item{warn_on_design}{Logical; emit non-fatal design-audit cautions.}
}
\value{
An object of class \code{"sw_multistage_power"}.
}
\description{
Repeatedly simulates a cumulative-intervention stepped-wedge trial and fits
the prespecified mixed model. Separate operating characteristics are reported
for A versus Control, incremental B (A+B versus A), total A+B versus Control,
multiplicity-adjusted component tests, and joint success of both component
tests.
}
\details{
Two power definitions are reported. \strong{Conditional power} uses only
replicates in which the planned analysis converged and produced the relevant
test. \strong{Failure-aware power} divides the same number of rejections by all
simulated trials, treating an unusable analysis as an unsuccessful trial.
Their difference reveals how much nominal performance depends on silently
discarding failed fits.
}
\examples{
\donttest{
design <- sw_multistage_design(
  clusters_per_sequence = rep(5, 5),
  a_start = c(2, 3, 4, 5, 6),
  b_start = c(6, 8, 7, 9, 10),
  n_periods = 10
)
assumptions <- sw_multistage_assumptions(
  baseline_prob = 0.15,
  treatment_or_a = 1.4,
  incremental_or_b = 1.3,
  delay_a = 1, delay_b = 1,
  icc = 0.05,
  n_per_cluster_period = 30
)
result <- power_multistage_swcrt(
  design, assumptions, nsim = 100, seed = 1
)
result
}
}
