% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multistage_analyze.R
\name{fit_multistage_model}
\alias{fit_multistage_model}
\title{Fit a cumulative A then A+B stepped-wedge analysis model}
\usage{
fit_multistage_model(
  data,
  formula = NULL,
  outcome = c("events", "n"),
  intervention_a = "a_effective",
  intervention_b = "b_effective",
  term_a = intervention_a,
  term_b = intervention_b,
  period = "period",
  cluster = "cluster_id",
  sequence = "sequence",
  adjust_sequence = FALSE,
  period_effect = c("categorical", "linear", "none"),
  link = c("logit", "identity"),
  nAGQ = 1,
  alpha = 0.05,
  multiplicity = c("holm", "bonferroni", "none")
)
}
\arguments{
\item{data}{Aggregated cluster-period data, typically from
\code{\link[=simulate_multistage_swcrt]{simulate_multistage_swcrt()}}.}

\item{formula}{Optional custom model formula. If supplied, the variable-name
and structural arguments are ignored, but \code{term_a} and \code{term_b} must name
the fitted coefficients representing A and incremental B.}

\item{outcome}{Length-two character vector \code{c(events, n)}.}

\item{intervention_a}{Name of the delayed A indicator.}

\item{intervention_b}{Name of the delayed B indicator.}

\item{term_a, term_b}{Coefficient names used to construct contrasts. By
default they equal \code{intervention_a} and \code{intervention_b}.}

\item{period}{Name of the calendar-period column.}

\item{cluster}{Name of the cluster identifier.}

\item{sequence}{Name of the sequence column.}

\item{adjust_sequence}{Logical; include a fixed sequence effect.}

\item{period_effect}{One of \code{"categorical"}, \code{"linear"}, or \code{"none"}.}

\item{link}{Binomial link, \code{"logit"} or \code{"identity"}.}

\item{nAGQ}{Adaptive Gauss-Hermite quadrature points.}

\item{alpha}{Two-sided significance level used for confidence intervals and
rejection indicators.}

\item{multiplicity}{Adjustment for the two component tests A and B. One of
\code{"none"}, \code{"holm"}, or \code{"bonferroni"}. The total A+B contrast is reported
separately and is not included in this adjustment family.}
}
\value{
A list containing the fitted model, a contrast table, raw and adjusted p-values, detailed fit status, convergence and singularity diagnostics, captured warnings, and a joint-success indicator for the A and B component tests. The \code{fit_status} field distinguishes hard GLMM errors, nonconvergence, converged fits with non-finite contrasts, and successful fits. Singularity is reported separately.
}
\description{
Fits a binomial cluster-random-intercept model with separate indicators for
the delayed effect of A and the delayed incremental effect of B. The default
model is
}
\details{
\code{cbind(events, n - events) ~ a_effective + b_effective + factor(period) + (1 | cluster_id)}.

The A coefficient compares A with control after the A wash-in period. The B
coefficient compares A+B with A after the B wash-in period. The total A+B
effect relative to control is the linear contrast \code{beta_A + beta_B} and its
standard error includes the covariance between the two estimates.
}
\examples{
\donttest{
design <- sw_multistage_design(
  clusters_per_sequence = rep(5, 4),
  a_start = c(2, 3, 4, 5), b_start = c(5, 7, 6, 8),
  n_periods = 8
)
assumptions <- sw_multistage_assumptions(
  baseline_prob = 0.15, treatment_or_a = 1.6,
  incremental_or_b = 1.4, delay_a = 1, delay_b = 1,
  icc = 0.05, n_per_cluster_period = 40
)
fit <- fit_multistage_model(
  simulate_multistage_swcrt(design, assumptions, seed = 1)
)
fit$contrasts
}
}
