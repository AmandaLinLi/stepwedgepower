% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multistage_assumptions.R
\name{sw_multistage_assumptions}
\alias{sw_multistage_assumptions}
\alias{print.sw_multistage_assumptions}
\title{Specify assumptions for a cumulative A then A+B stepped-wedge trial}
\usage{
sw_multistage_assumptions(
  outcome = "binary",
  baseline_prob = NULL,
  baseline_logit = NULL,
  treatment_or_a = NULL,
  treatment_effect_a = NULL,
  incremental_or_b = NULL,
  incremental_effect_b = NULL,
  delay_a = 0L,
  delay_b = 0L,
  icc = NULL,
  cluster_sd = NULL,
  period_effects = NULL,
  n_per_cluster_period = 20
)

\method{print}{sw_multistage_assumptions}(x, ...)
}
\arguments{
\item{outcome}{Outcome type. Currently \code{"binary"}.}

\item{baseline_prob}{Baseline probability: a scalar, a vector with one value
per sequence, or a sequence-by-period matrix. Supply this or
\code{baseline_logit}, not both.}

\item{baseline_logit}{Baseline linear predictor on the logit scale, with the
same permitted shapes as \code{baseline_prob}.}

\item{treatment_or_a}{Odds ratio for A versus control after the A delay.
Supply this or \code{treatment_effect_a}.}

\item{treatment_effect_a}{Effect of A on the log-odds scale.}

\item{incremental_or_b}{Incremental odds ratio for adding B to A after the B
delay. Supply this or \code{incremental_effect_b}. Defaults to one.}

\item{incremental_effect_b}{Incremental B effect on the log-odds scale.}

\item{delay_a, delay_b}{Non-negative integer wash-in periods for A and B.}

\item{icc}{Latent-scale intraclass correlation. Supply this or \code{cluster_sd}.}

\item{cluster_sd}{Cluster random-intercept standard deviation.}

\item{period_effects}{Numeric vector with one secular log-odds effect per
period. \code{NULL} means no secular trend.}

\item{n_per_cluster_period}{Cluster-period sample size: a scalar, a
sequence-by-period matrix, or a function returning one count per row.}

\item{x}{An \code{sw_multistage_assumptions} object.}

\item{...}{Unused.}
}
\value{
An object of class \code{"sw_multistage_assumptions"}.
}
\description{
Collects the data-generating assumptions for an asynchronous cumulative
intervention design created by \code{\link[=sw_multistage_design]{sw_multistage_design()}}. The binary-outcome
generating model is
}
\details{
\deqn{\mathrm{logit}(p_{ijt}) = \beta_{0,jt} + \gamma_t + b_i +
\beta_A A^*_{ijt} + \beta_B B^*_{ijt},}

where \verb{A*} becomes one after A has completed its prespecified delay and \verb{B*}
becomes one after B has completed its delay. Because B is never delivered
alone, \code{exp(beta_B)} is the incremental effect of adding B among clusters
already receiving A. The total A+B effect relative to control is
\code{exp(beta_A + beta_B)}.

A delay of zero means that the effect begins in the first active period. A
delay of one treats the first active period as wash-in and begins the effect
in the second exposure period.
}
\examples{
assumptions <- sw_multistage_assumptions(
  baseline_prob = 0.15,
  treatment_or_a = 1.40,
  incremental_or_b = 1.30,
  delay_a = 1,
  delay_b = 1,
  icc = 0.05,
  n_per_cluster_period = 30
)
assumptions
}
